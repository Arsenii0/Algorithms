#include "grid16x16.h"
#include "utils.h"

bool check_default_grid();
bool check_example_from_assignment();

/* Ideally, there should be more grid unit test cases (e.g. trying to select
 * invalid points)
 *
 * Also, for the ideal solution, it would be nice to have e2e tests with file
 * opening/reading and handling different errors (e.g, file opening errors, not
 * supported file format, content, etc.)
 */
